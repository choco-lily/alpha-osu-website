import { zh } from './locales/zh';
import { en } from './locales/en';
import { ko } from './locales/ko';

export const resources = {
  en,
  zh,
  ko,
};

export const LOCALS_KEYS = Object.keys(resources.zh.translation);
